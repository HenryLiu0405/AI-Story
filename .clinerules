# AI-Story Cline Rules

你是接入 DeepSeek V4 Pro 的 VS Code Copilot AI Coding 助手。本项目正在从"monolithic 小说创作 app"重构为"**面向小说创作者的 Agent Skills 套件 + 配套运行时**"。请严格按本规则工作。

## 1. 产品新定位（必须牢记）

AI-Story 不是单体应用，是三层结构：

- **`ai-story-core`**：Python 包（未来发布到 PyPI），提供所有 skill 共用的底层能力：Story Bible 读写、RAG 检索、Ollama/OpenAI/DeepSeek 多 provider 客户端、Pydantic schema。
- **`ai-story-skills`**：符合 Anthropic Agent Skills 开放标准的 skill 集合。每个 skill 独立、可单独安装到 Claude Code / Cursor / Codex CLI / 任何兼容 SKILL.md 的 AI 工具。
- **`ai-story-app`（原 frontend + backend）**：作者向的可视化 UI 和 skill 编排 runtime。给不写代码的小说作者用，未来 Tauri 打包成桌面 app。

竞品对照：天命（.NET 封闭生态）、StoryForge（Tauri 但闭环）、autonovel（全自动管线）都是封闭 app。**AI-Story 的独特性在于 skill 可以脱离 app 运行，能被任何 AI agent 工具调用**。这一点是项目的灵魂，任何改动都不能损害它。

## 2. 当前项目结构（重构期）

```
ai-story/
├── core/                    # ai-story-core 包（新建）
│   ├── ai_story_core/
│   │   ├── story_bible.py
│   │   ├── rag.py
│   │   ├── llm.py
│   │   └── schemas.py
│   └── pyproject.toml
├── skills/                  # ai-story-skills（新建）
│   ├── continuity-guardian/
│   │   ├── SKILL.md
│   │   ├── scripts/check.py
│   │   └── resources/
│   ├── story-bible-curator/
│   ├── character-interview/
│   └── anti-ai-slop-detector/
├── app/                     # 原 frontend/ + backend/ 整体移入
│   ├── frontend/
│   └── backend/
├── ARCHITECTURE.md
├── SKILL_AUTHORING_GUIDE.md
├── OPEN_STORY_BIBLE_FORMAT.md
└── spec.md
```

旧的 `frontend/` 和 `backend/` 不要删除，移入 `app/` 子目录即可。所有现有功能继续可用，逐步把核心逻辑下沉到 `core/`。

## 3. 第一批 skill（仅限这 4 个，未经用户确认不要新增）

| Skill | description | 触发 |
|---|---|---|
| `continuity-guardian` | 检查章节文本与已有设定的冲突 | 章节写完 |
| `story-bible-curator` | 从文本提取结构化设定条目 | 章节写完、整理设定 |
| `character-interview` | 以角色身份回答问题 | 测试角色声音 |
| `anti-ai-slop-detector` | 检测 AI 写作痕迹并提改写建议 | 任意时刻 |

## 4. 默认行为：每个新需求先分类

收到任何新功能请求，**首先回答这个问题**：

1. 这是一个独立的 AI 动作（输入文本，输出结构化结果）？→ **应该做成 skill**，放在 `skills/` 下。
2. 这是数据存储、UI 展示、跨 skill 编排？→ **应该做在 `app/`**，作为 runtime UI 能力。
3. 这是所有 skill 都会用到的底层能力？→ **应该做在 `core/`**，作为共享库。

判断不清就停下来问用户，不要默认归到 `app/backend/`。

## 5. Skill 开发规范

每个 skill 必须满足：

- 根目录有 `SKILL.md`，YAML frontmatter 包含 `name` 和 `description`。description 必须具体、单一职责、能直接被 Claude 用于路由。
- 业务逻辑在 `scripts/` 下，纯 Python，通过 `import ai_story_core` 调用共享能力。
- 输出严格 JSON 并用 Pydantic schema 校验，无法判断时返回空数组或 `confidence: 0`，不要编造。
- 不直接读写数据库，统一通过 `ai_story_core.story_bible` 模块读写 `.aistory/bible.json`（详见 `OPEN_STORY_BIBLE_FORMAT.md`）。
- 不依赖 `app/backend` 的任何 FastAPI 路由——skill 必须能在用户没有运行 backend 的情况下独立工作。
- 完整流程参见 `SKILL_AUTHORING_GUIDE.md`。
- **详细规范、模板和反模式参见 SKILL_AUTHORING_GUIDE.md，新建 skill 前必须完整阅读。**

## 6. Core 包开发规范

- 不依赖任何 web 框架。`core/` 是纯库，没有 FastAPI / Vue / React 依赖。
- 不假设数据库存在。所有持久化通过文件（默认 `.aistory/bible.json`）或调用方传入的句柄。
- AI provider 调用统一通过 `ai_story_core.llm` 模块，支持 OpenAI / DeepSeek / Ollama 切换。
- 任何新增能力必须 type-hinted，必须有 Pydantic schema。

## 7. App（runtime UI）开发规范

现有 React + FastAPI 代码继续维护，但定位转变：

- 不再新增"独立完成某 AI 任务"的 API（这种工作应该归到 skill）。
- 新增 API 只为：(a) skill 的编排和调度；(b) 数据可视化；(c) 文件管理；(d) 设置面板。
- 现有 `backend/app/services/` 下的服务，逐步下沉到 `core/` 或拆为 skill。但不要一次性大重构，新功能走新结构即可。
- 前端继续按现有规范，但 App.tsx 必须开始拆分。新 tab 必须是独立组件。

## 8. AI 行为约定

每次接到任务前：

1. 先读 `ARCHITECTURE.md` 确认理解三层结构。
2. 判断任务属于 core / skill / app 哪一层。
3. 给出文件清单和最小改动方案，**等用户确认再写代码**。
4. 不擅自跨层修改（比如做 skill 时不要顺手改 app/backend）。

## 9. 隐私与安全

- 小说稿件、用户 API Key、上传文档都视为敏感数据。
- 日志中不打印稿件全文、API Key、AI 完整返回。
- `.aistory/bible.json` 默认存储在用户项目目录，不上传任何远端。
- skill 的 description 中不要泄露用户数据。

## 10. 验收标准

每个改动完成时输出：

1. 修改的文件清单和所属层（core / skill / app）。
2. 是否新增了跨层依赖（如果有，标红警告）。
3. 建议运行的检查命令。
4. 是否破坏了"skill 独立可运行"原则。

## 11. 禁止事项

- 不要在 `app/backend/` 新增功能后再考虑拆 skill——顺序反了。
- 不要让 skill 依赖 FastAPI、SQLAlchemy 或任何 web 框架。
- 不要让 skill 之间互相调用（skill 是平级的，需要协作时通过 `app/` 的编排层）。
- 不要承诺 OpenAI / Anthropic / DeepSeek / Ollama 之外的兼容性，除非用户明确要求。
- 不要在 README 或宣传材料里写"取代人类作者"之类的话——AI-Story 是助手不是枪手。