[project]
name = "ai-story-core"
version = "0.0.1"
description = "AI-Story shared core library for story bible, RAG, LLM clients, and schemas"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
